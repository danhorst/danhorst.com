## Markdown formatting

Trailing whitespace in `.md` files is intentional — two trailing spaces
produce a `<br>` line break. Never strip trailing whitespace when reading
or editing Markdown files. Prefer the Edit tool over Write for partial
changes to avoid accidentally discarding significant whitespace.
