---
title: "Common Linux Commands"
date: 2018-09-10T16:20:15-04:00
categories:
 - Reference
published: false
---

## Toolkit
- `tree` over `ls` (for high-level overview)
- `htop` over `top`
- `vim` over `vi`

## Remote Hosts

ssh 

ssh-copy-id

ssh TUNNEL

scp

## Traversal

## Connectivity

## Tarballs

tar -cvzf ARCHIVE.tar.gz /souce/directory

tar -xvzf ARCHIVE.tar.gz

tar -xvzf ARCHIVE.tar.gz -C /path/to/destination
